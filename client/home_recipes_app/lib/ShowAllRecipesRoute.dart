import 'package:flutter/material.dart';

class ShowAllRecipesRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Show all recipes Route"),
      ),
      body: Center(
        child: Text('Implement me!'),
      ),
    );
  }
}