import 'package:flutter/material.dart';

class ListIngredientsInKitchen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("List Ingredients In Kitchen Route"),
      ),
      body: Center(
        child: Text('Implement me!'),
      ),
    );
  }
}