import 'package:flutter/material.dart';

class GetIngredientsForRecipes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Get Ingredients For Recipes Route"),
      ),
      body: Center(
        child: Text('Implement me!'),
      ),
    );
  }
}