import 'package:flutter/material.dart';

class AddRecipeRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Add Recipe Route"),
      ),
      body: Center(
          child: Text('Implement me!'),
      ),
    );
  }
}